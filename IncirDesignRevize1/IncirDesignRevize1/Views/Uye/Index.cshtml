@{
	ViewData["Title"] = "Üye Alanı";
}
<div class="container mt-4">
    <div class="card shadow border-primary">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">
                <i class="bi bi-person-circle me-2"></i>Üye Alanı
            </h4>
        </div>
        <div class="card-body">
            <div class="alert alert-info">
                <i class="bi bi-info-circle me-2"></i>
                Bu sayfayı <strong>giriş yapmış tüm kullanıcılar</strong> görebilir.
            </div>
            <p>Hoş geldiniz! Burası üyelere özel alandır.</p>
        </div>
    </div>
</div>